<!DOCTYPE html>
<html>
<body>
  <main class="flex flex-col lg:flex-wrap lg:w-full border-2 border-red-500">
    <div class="flex flex-col w-1/5 lg:justify-center lg:my-4 lg:mr-auto  lg:ml-80 lg:w-3/4">
      <% if @movie_data.present? %>
        <img src="<%= @movie_data['Poster'] %>" alt="Movie Poster" class="mr-5">
      <% else %>
        <p>No movie found.</p>
      <% end %>

      <div class="flex-col lg:mt-2 lg:mb-4">
        <div class="lg:mr-60">
          <h1 class="lg:text-3xl"><%= @movie_data['Title']%></h1>
          <p class="text-slate-700"><%= @movie_data['Year'] %> | <%= @movie_data['Runtime']%> | <%= @movie_data['Rated'] %> | <%= @movie_data['Genre'] %></p>
        </div>
        <div class="lg:mt-4 lg:mr-60">
          <h4 class="font-bold">Director</h4>
          <p><%= @movie_data['Director'] %></p>
        </div>
        <div class="lg:mt-4 lg:mr-60">
          <h4 class="font-bold">Actors</h4>
          <p><%= @movie_data['Actors'] %></p>
        </div>  
        <div class="lg:mt-4 lg:mr-60">
          <h4 class="font-bold">Awards</h4>
          <p><%= @movie_data['Awards'] %></p>
        </div>  
        <div class="lg:mt-4 lg:mr-60 overflow-scroll">
          <h4 class="font-bold">Summary</h4>
          <p class="overflow-scroll"><%= @movie_data['Plot'] %></p>
        </div>
      </div>
    </div>
      <div class="flex flex-wrap justify-center lg:mb-10 lg:mt-4 lg:mr-72  lg:ml-80 lg:w-3/4">
        <%= render partial: 'form'%>
      </div>
    </div>
  </main>

</body>
</html>
